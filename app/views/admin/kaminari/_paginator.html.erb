<%# The container tag
  - available local variables
    current_page:  a page object for the currently displayed page
    total_pages:   total number of pages
    per_page:      number of items to fetch per page
    remote:        data-remote
    paginator:     the paginator that renders the pagination tags inside
%>

<%= paginator.render do %>
  <div class="flex w-full justify-center gap-[var(--spacer-2)]" role="navigation" aria-label="Pagination" >
    <%= first_page_tag %>
    <%= prev_page_tag %>
    <menu class="hidden! lg:flex! lg:grow! lg:justify-center! lg:gap-[var(--spacer-2)]!">
      <% each_page do |page| %>
        <% if page.display_tag? %>
          <li>
            <%= page_tag page %>
          </li>
        <% end %>
      <% end %>
    </menu>
    <%= next_page_tag %>
    <%= last_page_tag %>
  </div>
<% end %>
